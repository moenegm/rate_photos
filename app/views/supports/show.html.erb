</br>
</br>
<%= link_to "Back to supports", supports_path, class: "nounderline bi bi-arrow-left mb-3", style: "font-size: 1.2rem; color: cornflowerblue;" %>
</br>
</br>
<div class="card mb-5">
  <div class="card-header">
    <div class="row">
      <div class="col">
        <%= @support.support_type %>
      </div>
      <div class="col text-center">
        < <%= @support.id %> >
      </div>
      <div class="col text-end">
        <% user = User.find(@support.user_id) %>
        <% if user.admin %>
            <span title = "Admin" > <i class="bi bi-patch-check-fill" style= "font-size: 1.2rem; color: orange;"></i></span>
        <% elsif user.verified %>
            <span title = "Verified" ><i class="bi bi-patch-check-fill" style= "font-size: 1.2rem; color: cornflowerblue"></i></span>
        <% end %>
        <%= link_to "#{user.first_name} #{user.last_name}", user_path(@support.user_id), class:"nounderline"%>
      </div>
    </div>
  </div>

  <div class="card-body">
    <%= @support.body %>
  </div>

  <div class="card-footer text-muted">
    <div class="row">
      <div class="col">
        <%#= link_to "Show this support", support, class:"btn btn-outline-secondary btn-sm" %>
      </div>
      <div class="col text-center">
        < <%= @support.id %> >
      </div>
      <div class="col text-end">
        Posted: <%= time_tag @support.created_at, "data-local": "time-ago" %>
      </div>
    </div>
  </div>
</div>

<% if user_signed_in? && @support.user_id == current_user.id%>
  <%= link_to 'Edit', edit_support_path(@support), class:"btn btn-outline-secondary" %>
  <%= link_to 'Delete', @support, :method => :delete, :confirm => "Are you sure?", class:"btn btn-outline-danger" %>
<% end %>
